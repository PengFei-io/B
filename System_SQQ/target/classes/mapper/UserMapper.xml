<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.io.dao.IUserDAO">

    <insert id="saveUser" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        <!--<selectKey  keyColumn="id" resultType="int" keyProperty="id" order="AFTER" >-->
        <!--select last_insert_id()-->
        <!--</selectKey>-->
        insert into
        t_user(name,password,sex,email,birthDay,photo)values(#{name},#{password},#{sex},#{email},#{birthDay},#{photo})
    </insert>

    <update id="updateUser" parameterType="User">
        update t_user set name=#{name},sex=#{sex},email=#{email},birthDay=#{birthDay},photo=#{photo} where id=#{id}
    </update>

    <delete id="deleteByUserId" parameterType="int">
        delete from t_user where id=#{id}
    </delete>

    <select id="queryUserByPage" resultType="User">
        select id,name,sex,email,birthDay,photo from t_user  <if test="column != null">where ${column} like
        '%${value}%'
    </if> limit ${pageSize} offset ${(pageNow-1)*pageSize}
    </select>
    <select id="queryCount" resultType="int">
        select count(*) from t_user
        <if test="column != null">where ${column} like '%${value}%'</if>
    </select>

    <select id="queryUserById" resultType="User">
        select id,name,sex,email,birthDay,photo from t_user where id=#{id}
    </select>
    <select id="queryUserByNameAndPassword" resultType="User">
        select id,name,sex,email,birthDay,photo from t_user where email=#{email} and password=#{password}
    </select>
</mapper>
